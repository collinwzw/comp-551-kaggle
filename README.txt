make sure the origianl data is store in the same direcoty with in a fold called "all"

if you want to load a processed data, store it in the same directory and name it as train_data.npy for train. test_data.npy for test

The trained model weights will be save to a file named model_1.h5


CNN jupyter notebook is for training a model with convolutional neural network

verify jupyter notebook is for verfing the result by ploting same images out and the predicition result to see if they match

